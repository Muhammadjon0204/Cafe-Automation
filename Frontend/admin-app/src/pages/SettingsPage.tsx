import { PlaceholderPage } from './PlaceholderPage';

export function SettingsPage() {
  return <PlaceholderPage title="Settings" note="Working hours, menu categories, and base parameters — optional, later." />;
}
