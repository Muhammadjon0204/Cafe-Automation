# Relative path — must stay same-origin so vite.config.ts's dev proxy
# actually intercepts it (an absolute http://localhost:5082 URL would bypass
# the proxy and hit the backend's missing-CORS wall directly).
VITE_API_BASE_URL=/api
